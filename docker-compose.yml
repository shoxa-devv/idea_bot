services:
  idea-bot:
    build: .
    container_name: idea-bot
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./bot_database.db:/app/bot_database.db
